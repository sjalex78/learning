// import { render, screen } from "@testing-library/react";
// import FeedbackCard from "./FeedbackCard";

test("render a feedback detail", () => {
  // render(<FeedbackCard name="Ace Feedback" />);
  // const feedbackName = screen.queryByRole("heading");
  // expect(feedbackName).toBeInTheDocument();
});
// NOTE: look up and reserach how to test components that need a prop passed to them..
// Pair to work this out
